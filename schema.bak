Create table users(username varchar(20) primary key, passcode varchar(20))
Create table comments(pid int NOT NULL, username varchar(20), comment mediumtext, posted_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP)
Create table papers(id int auto_increment primary key, title varchar(1000))
insert into users values('admin', 'admin')
Create table meetinglogs(id int auto_increment primary key, username varchar(20), meeting_minutes mediumtext, meeting_date date)
Insert into meetinglogs(username, meeting_minutes, meeting_date) values('ramu', 'bip bip', 2020-03-03)